import { http } from '@/utils/http'

/**
 * 登录表单
import {  } from "module";

/**
 * 查资料
 * @returns SearchApiResponse 验证码
 */
export const getChipDetail = (params: any) => {
  return http.get(`/api/v1/products/${params.id}`)
}
